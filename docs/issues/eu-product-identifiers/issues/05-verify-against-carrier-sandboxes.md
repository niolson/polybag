# Verify against the carrier sandboxes, resolve `NO`/`NA`, answer the FedEx form

Status: ready-for-human
Category: research
Repo: `polybag`

> *This was generated by AI during triage.*

## Parent

[PRD](../PRD.md). Runs after `02` and `03`; the readiness form does not wait for them.

## What needs a person

1. **Answer the FedEx Compatible readiness form** with the date in the PRD, 15 October
   2026 for the Ship API. Do this first; it is what FedEx actually asked for and it does
   not depend on any code.
2. **Ask the FedEx integrator mailbox** to confirm `"NA"` (the Ship API docs) over
   `"NO"` (the notice) as the placeholder for a missing standard identifier. `02` ships
   `"NA"`; if FedEx says otherwise, change the constant and record here who said so.
3. **FedEx sandbox:** buy one label to an EU consumer address with all three identifiers
   and one with the placeholder; read the `fedex-validation` log and the returned
   commercial invoice to confirm the identifiers appear on the document. Check whether
   the sandbox now rejects the rate request's `Merchandise` placeholder commodity for an
   EU destination; if it does, `02` widens to the rate request.
4. **UPS:** find out whether CIE has the fields yet (the August guidance says production
   does not). If it does, buy one label to an EU consumer address and confirm the
   invoice; if it does not, record the date UPS names for production and set a reminder
   to re-run. Confirm the JSON form of `productIdentifierExemptIndicator` and the
   `ShipperType`/`ConsigneeType` codes against the schema, and correct `03` if the
   assumptions there are wrong.
5. **USPS:** check the developer portal and the API changelog once a month until
   something appears, and open a `06` when it does. Once, now: send one sandbox label
   request with a made-up key on a `contents[]` item and record whether USPS rejects
   it or ignores it — it decides whether a future field can be sent ahead of the spec.
6. **Tenants:** every client shipping to EU consumers needs a manufacturer part number
   on each product before 1 November. Draft the notice — what the field is, where to
   enter it, that a Database import can map it — and send it as soon as `01` is deployed.

## Acceptance criteria

- [ ] FedEx form submitted; date recorded here
- [ ] `NO`/`NA` answered and the adapter constant matches
- [ ] FedEx sandbox label with identifiers on the invoice, evidence in `.scratch/`
- [ ] UPS: either a sandbox label with identifiers, or UPS's production date recorded
      with a follow-up date
- [ ] Tenant notice sent

## Comments

- 2026-09-17 — opened from the FedEx Compatible notice and the UPS v1.0 guidance.

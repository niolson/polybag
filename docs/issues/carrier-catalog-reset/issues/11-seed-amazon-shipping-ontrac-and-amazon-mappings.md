# Seed Amazon Shipping and OnTrac, and Amazon's known service mappings

Status: needs-triage

Repo: `polybag`

> *This was generated by AI during triage.*

## Parent

[PRD](../PRD.md), *Catalog* and *Source mapping*. ADR-0006 decisions 1 and 2, and the
trade-off: mapping becomes authorization.

## What to build

HITL. Amazon Shipping and OnTrac become carriers, and the Amazon identifiers already
seen are seeded as authored mappings in the table `14` created. From `13` on, a seeded
mapping lets automation buy the service on any method that lists it. So the maintainer
signs off the table below before it is seeded.

- **Catalog.**
  - Seed `Amazon Shipping` with *Amazon Shipping Ground*, `service_code`
    `std-us-swa-mfn`, the code Amazon returns for it in the sandbox. `15` registers
    its direct adapter under this name.
  - Seed `OnTrac` (no adapter) with *OnTrac Ground*. Take its `service_code` from OnTrac's
    own API documentation when seeding, not from Amazon's identifier and not from memory,
    so a later direct integration needs no remapping.
  - Nothing else is known for either. Across every capture, OnTrac has one identity
    (`ONTRAC_MFN_GROUND`, eligible in production). Amazon Shipping has one
    (`std-us-swa-mfn`, seen only in the sandbox; production has never quoted it for us).
    `ONTRAC_MFN_SUNRISE`, `ONTRAC_MFN_GOLD` and `DHL_PARCEL_GROUND` appear in the repo
    only as test fixtures.
  - Author USPS *Parcel Select* (`PARCEL_SELECT`). The direct USPS adapter already
    accepts it.
  - **UPS Ground Saver stays two services, one per weight band**, because every source
    sells it that way with its own code for each band:

    | Sold through | Under 1 lb | 1 lb and over |
    |---|---|---|
    | UPS direct | `92` | `93` |
    | Shopify | `ups_shipping:92` | `ups_shipping:93` |
    | Amazon | `UPS_PTP_SUREPOST_L` | `UPS_PTP_SUREPOST_H` |

    Shopify's admin toggles them separately, as *UPS® Ground Saver (<1 lb)* and *UPS®
    Ground Saver*. `CarrierSeeder` names both *UPS Ground Saver*, so the method form shows
    two identical entries. Rename them *UPS Ground Saver (under 1 lb)* and *UPS Ground
    Saver (1 lb and over)*: in a one-off migration for existing rows, and in the seeder
    for new installs. The seeder must not rename on every start, because it would undo
    an Admin's rename. A method may list one band without the other.
  - **Author UPS Ground Saver Media**: UPS code `95`, 1 lb and over, with
    `required_contents = media` (`02`). It reaches PO Boxes and military addresses,
    because its last mile is USPS Media Mail. That requirement is what makes it safe to sell.
    PolyBag's own product check guards it for every source, the same check that is all
    direct USPS Media Mail relies on. Amazon lists it as `UPS_PTP_SUREPOST_MEDIA` ("UPS
    Ground Saver Media"). Both production captures refused it only for the 16 oz minimum.
    - The direct UPS adapter does not claim `95` until a UPS response has shown it. The
      UPS account we test with does not have Ground Saver enabled at all.
    - Shopify does not sell it: its *Preferred services* screen has no Ground Saver Media.
    - Ground Saver BPM (`94`) is not authored, as no Bound Printed Matter is (ADR-0006
      decision 11).
  - These carriers and services go in `CarrierSeeder`, found by name and marked
    `is_system` (`05`), so their names are fixed. Deactivate rather than delete one that is not wanted: the
    reference-data sync on every start would recreate a deleted row.
- **Mappings are seeded once, behind a marker.** A mapping authorizes from `13` on, and the
  reference-data sync would restore one an Admin had removed on the next start. The sync
  writes the signed-off rows as a named batch, using the marker `09` builds, and never
  writes that batch again. Not by migration: migrations run before the sync, so on a
  fresh install there would be no service rows to map (ADR-0006 decision 2, 2026-09-25
  amendment).
- **Remove `04`'s fallback** that gated `USPS_PTP_MM` by identifier. The seeded mapping
  covers it.
- **Take `UPS_PTP_SUREPOST_MEDIA` off the adapter's drop list.** Its mapping to UPS Ground
  Saver Media carries the media requirement, so a Package that does not qualify never
  sees it. The list is left with `UPS_PTP_SUREPOST_BPM` (dropped) and `USPS_PTP_BPM`
  (attended-only).
- **Approvals are unchanged.** They are still keyed on Amazon's identifiers until `13`.

### Proposed seed mappings, for sign-off

These are drawn from the captured `getRates` responses. They cover the carriers in
`ServiceApprovals::US_BUY_SHIPPING_CARRIERS`. Several identifiers alias one service
where they differ only by packaging, and packaging stays on the rate (ADR-0005).

| Amazon identifiers | Maps to |
|---|---|
| `AMZN_US` `std-us-swa-mfn` | Amazon Shipping Ground (new) |
| `ONTRAC` `ONTRAC_MFN_GROUND` | OnTrac Ground (new) |
| `USPS_PTP_GAL`, `_GAH`, `_GAC`, `_FC_CUSTOMS` | USPS Ground Advantage |
| `USPS_PTP_FC` ("USPS First Class") | USPS Ground Advantage — **check** |
| `USPS_PTP_PRI`, `_PRI_CUBIC`, `_PRI_CUSTOMS`, `_PRI_FRE`, `_PRI_LFRE`, `_PRI_PFRE`, `_PRI_SFRB`, `_PRI_MFRB`, `_PRI_LFRB`, `_PRI_LFRB_CUSTOMS` | USPS Priority Mail |
| `USPS_PTP_EXP`, `_EXP_CUSTOMS`, `_EXP_FRE`, `_EXP_LFRE`, `_EXP_PFRE` | USPS Priority Mail Express |
| `USPS_PTP_PRI_INTL`, `_PRI_FRE_INTL`, `_PRI_SFRB_INTL`, `_PRI_MFRB_INTL`, `_PRI_LFRB_INTL` | USPS Priority Mail International |
| `USPS_PTP_EXP_INTL`, `_EXP_FRE_INTL` | USPS Priority Mail Express International (`09`) |
| `USPS_PTP_FC_INTL` | USPS First-Class Package International Service (`09`) |
| `USPS_PTP_MM` | USPS Media Mail (`02`) |
| `USPS_PTP_PSBN` | USPS Parcel Select (new) |
| `UPS_PTP_GND` | UPS Ground (`03`) |
| `UPS_PTP_3DAY_SELECT` | UPS 3 Day Select (`12`) |
| `UPS_PTP_2ND_DAY_AIR`, `_2ND_DAY_AIR_SAT` | UPS 2nd Day Air (`02`) — Saturday **check** |
| `UPS_PTP_NEXT_DAY_AIR`, `_NEXT_DAY_AIR_SAT` | UPS Next Day Air (`01`) — Saturday **check** |
| `UPS_PTP_NEXT_DAY_AIR_SAVER` | UPS Next Day Air Saver (`13`) |
| `UPS_PTP_SUREPOST_L` | UPS Ground Saver (under 1 lb) (`92`) |
| `UPS_PTP_SUREPOST_H` | UPS Ground Saver (1 lb and over) (`93`) |
| `UPS_PTP_SUREPOST_MEDIA` | UPS Ground Saver Media (`95`, new) |
| `UPS_EXPRESS_INTL` / `_EXPEDITED_INTL` / `_SAVER_INTL` / `_STANDARD_INTL` | UPS Worldwide Express (`07`) / Expedited (`08`) / Saver (`65`) / Standard (`11`) |
| `FEDEX_PTP_GROUND` | FedEx Ground |
| `FEDEX_PTP_HOME_DELIVERY` | FedEx Home Delivery |
| `FEDEX_PTP_SMARTPOST` | FedEx Ground Economy |
| `FEDEX_PTP_EXPRESS_SAVER`, `_EXPRESS_SAVER_ONE_RATE` | FedEx Express Saver |
| `FEDEX_PTP_SECOND_DAY`, `_SECOND_DAY_ONE_RATE`, `_SECOND_DAY_SAT`, `_SEC_DAY_ONE_RATE_SAT` | FedEx 2Day — Saturday **check** |
| `FEDEX_PTP_SECOND_DAY_AM`, `_SECOND_DAY_AM_ONE_RATE` | FedEx 2Day A.M. |
| `FEDEX_PTP_STANDARD_OVERNIGHT`, `_STANDARD_OVERNIGHT_ONE_RATE` | FedEx Standard Overnight |
| `FEDEX_PTP_PRIORITY_OVERNIGHT`, `_PRIORITY_OVERNIGHT_ONE_RATE`, `_PRI_OVERNIGHT_SAT`, `_PRI_OVERN_ONE_R_SAT` | FedEx Priority Overnight — Saturday **check** |

Left unmapped:

- `USPS_PTP_BPM`: Bound Printed Matter is not authored, and the offer is attended-only
  (`04`).
- `UPS_PTP_SUREPOST_BPM`: still dropped (`04`). Ground Saver BPM (`94`) is not authored.
- `UPS_PTP_GROUNDSAVER` ("UPS Ground Saver"). It is not tied to a weight band. Both
  production captures refused it only for the delivery promise, at 8 oz and 12 oz, while
  `UPS_PTP_SUREPOST_H` was refused for the 16 oz minimum. So it fits neither band, and an
  Amazon mapping names exactly one service. Its name matches what Shopify's admin calls
  `93`. Do not map it to `93` on the name alone: Amazon's own refusals show it accepts
  parcels under a pound. Packers can still pick it, and automation buys it only under
  *any service*. Revisit if an eligible offer shows what it is, or if Amazon replaces
  `_L`/`_H` with it.
- `DHLMX` `DHLMX_PTP_PACKAGE_EXPRESS` ("DHL Express International"). The carrier ID
  suggests DHL's Mexico entity, and it has only ever appeared as ineligible. It is not
  safe to treat it as DHL Express Worldwide until an eligible offer shows what it is.

The carriers outside the US list (4PX, China Post, Yanwen and others) are left for the
page.

Questions for the sign-off:

- **Saturday variants.** Mapping them to the base service means a method that lists
  UPS Next Day Air lets automation buy the Saturday variant, which usually costs more.
  The due-by date already decides whether the extra day matters.
- **`USPS_PTP_FC`.** First-Class Package became Ground Advantage in 2023.

## Acceptance criteria

- [ ] The maintainer has signed off the seed table, and the questions above are answered
      in this file
- [ ] An Amazon offer for a seeded identifier arrives named as its `CarrierService`, for
      example OnTrac Ground under the `OnTrac` carrier
- [ ] A seeded mapping removed on the mapping page stays removed after
      `app:sync-reference-data`
- [ ] OnTrac Ground's `service_code` is OnTrac's own, with its source noted in the seeder
- [ ] The two Ground Saver services are named by weight band on the method form, and an
      Admin's rename of either survives `app:sync-reference-data`
- [ ] An Amazon `UPS_PTP_SUREPOST_MEDIA` offer is shown as UPS Ground Saver Media for a
      qualifying Package and dropped, with no `ShippingOffer`, for one that does not
      qualify
- [ ] Approvals behave as before

## Blocked by

- [`05`](05-lock-system-carrier-names.md), for system carriers
- [`14`](14-one-source-mapping-table.md), which creates the table and moves the mappings
  into it

## Comments

- **2026-09-24** — Second review: moving the mappings, removing `MAPPING_LOCK` and making
  the page Admin-only moved to `14`, so this is now the seed and its sign-off. The seed
  became a one-shot migration, because a mapping removed by an Admin would otherwise come
  back on the next start. Service codes follow the carriers' own. `DHLMX` stays
  unmapped.
- **2026-09-24** — UPS Ground Saver stays two services. UPS, Shopify's admin (which
  toggles *Ground Saver (<1 lb)* and *Ground Saver* separately) and Amazon all split it by
  weight, each with its own code per band. The rows get distinct names. Ground Saver Media
  (`95`) is authored with the media requirement, so `UPS_PTP_SUREPOST_MEDIA` maps and
  leaves the drop list. `UPS_PTP_GROUNDSAVER` stays unmapped because it is not tied to a
  weight band.
